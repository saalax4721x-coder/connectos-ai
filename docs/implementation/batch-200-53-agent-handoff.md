# Agent Handoff Contract

Handoffs preserve goal, intent, constraints, evidence, provenance, permissions, memory references, current stage, expected output, and failure context. Agents never silently discard uncertainty or authorization state.