# ConnectOS Workflow Contracts

Workflow definitions support sequential, parallel, conditional, loop, retry, approval, handoff, schedule, trigger, and rollback semantics.

Every workflow step must declare inputs, outputs, permissions, timeout, retry policy, failure behavior, and audit metadata. Sensitive external actions remain approval-gated.
