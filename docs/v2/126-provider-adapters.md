# Provider Adapters

Defines stable interfaces for external search, maps, calendar, email, contacts, CRM, data, and model providers. Development adapters must be clearly marked and must never masquerade as production integrations.