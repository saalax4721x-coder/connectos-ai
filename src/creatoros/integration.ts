export interface CreatorOSArtifact{artifactId:string;type:'film'|'campaign'|'animation'|'script'|'design'|'music';ownerId:string;status:string;}
export interface CreatorOSBridge{findDistributors:boolean;findClients:boolean;findPartners:boolean;findCollaborators:boolean;}
