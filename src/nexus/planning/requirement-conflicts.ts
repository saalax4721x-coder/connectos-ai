export interface RequirementConflict{left:string;right:string;reason:string;} export function findConflicts(items:Array<{id:string;value:string}>):RequirementConflict[]{const out:RequirementConflict[]=[];for(let i=0;i<items.length;i++)for(let j=i+1;j<items.length;j++)if(items[i].value&&items[j].value&&items[i].value===items[j].value)out.push({left:items[i].id,right:items[j].id,reason:'duplicate value'});return out;}