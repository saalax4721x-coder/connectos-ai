import type {GraphRepository} from './repository'; import type {GraphNode} from './node'; import type {Relationship} from './relationship'; export class InMemoryGraph implements GraphRepository{private n=new Map<string,GraphNode>();private e=new Map<string,Relationship>();getNode(id:string){return this.n.get(id);}putNode(x:GraphNode){this.n.set(x.id,x);}getEdges(id:string){return [...this.e.values()].filter(x=>x.from===id||x.to===id);}putEdge(x:Relationship){this.e.set(x.id,x);}}
