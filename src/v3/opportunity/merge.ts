import type {Opportunity} from './opportunity'; export const mergeOpportunity=(a:Opportunity,b:Opportunity):Opportunity=>({...a,description:a.description||b.description,personIds:[...new Set([...a.personIds,...b.personIds])],companyIds:[...new Set([...a.companyIds,...b.companyIds])],sourceIds:[...new Set([...a.sourceIds,...b.sourceIds])],confidence:Math.max(a.confidence,b.confidence),updatedAt:new Date().toISOString()});
