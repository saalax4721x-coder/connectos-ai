import type {Opportunity} from './opportunity'; export interface Qualification{qualified:boolean;reasons:string[];missing:string[];} export const qualify=(o:Opportunity):Qualification=>{const missing:string[]=[];if(!o.title)missing.push('title');if(!o.sourceIds.length)missing.push('source');if(o.confidence<.5)missing.push('confidence');return {qualified:missing.length===0,reasons:missing.length?[]:['minimum evidence present'],missing};};
